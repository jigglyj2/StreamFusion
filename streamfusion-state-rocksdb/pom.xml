<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>tech.streamfusion</groupId>
        <artifactId>streamfusion-parent</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>streamfusion-state-rocksdb</artifactId>
    <packaging>jar</packaging>
    <name>StreamFusion : Native RocksDB State Backend</name>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>${exec.maven.plugin.version}</version>
                <executions>
                    <execution>
                        <id>cargo-build</id>
                        <phase>generate-resources</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <executable>cargo</executable>
                            <arguments>
                                <argument>build</argument>
                                <argument>--release</argument>
                                <argument>--locked</argument>
                            </arguments>
                            <environmentVariables>
                                <CARGO_TARGET_DIR>${project.build.directory}/rust</CARGO_TARGET_DIR>
                                <CARGO_BUILD_JOBS>4</CARGO_BUILD_JOBS>
                                <RUSTFLAGS>-C target-cpu=native -C force-frame-pointers=yes -C debuginfo=1</RUSTFLAGS>
                            </environmentVariables>
                        </configuration>
                    </execution>
                    <execution>
                        <id>cargo-test</id>
                        <phase>test</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${skipTests}</skip>
                            <executable>cargo</executable>
                            <arguments>
                                <argument>test</argument>
                                <argument>--locked</argument>
                            </arguments>
                            <environmentVariables>
                                <CARGO_TARGET_DIR>${project.build.directory}/rust</CARGO_TARGET_DIR>
                                <CARGO_BUILD_JOBS>4</CARGO_BUILD_JOBS>
                            </environmentVariables>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>${project.build.directory}/rust/release</directory>
                <targetPath>META-INF/native/linux-x86_64</targetPath>
                <includes>
                    <include>libstreamfusion_state_rocksdb.so</include>
                </includes>
            </resource>
        </resources>
    </build>
</project>
